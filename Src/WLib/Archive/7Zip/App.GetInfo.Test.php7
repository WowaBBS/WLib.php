<?
  for($F=__FILE__; $F;) if(@include($F=DirName($F)).'/Using.php') break;
  
  $Loader->GetLogger()->Add(__FILE__.'.log');
  
  $App=$Loader->Create_Object('/Archive/7Zip/App');
  
  $ArchFile = __DIR__.'.7z';
//$ArchFile = __DIR__.'/BigFile.dat';
  $Args=[
  //'Password'     =>'Test'           ,
  //'Output'       =>'InfoOutput.txt' ,
  //'Output'       =>True,
  //'Error'        =>'InfoError.txt'  ,
  # 'Async'        =>True ,
  # 'Args'         =>['-mx9'],
  ];
  $Info=$App->GetInfo($ArchFile, $Args);
  $Loader->Log('Debug', 'Info:')->Debug($Info);
  $Loader->Log('Debug', 'UnpackedSize: ', $App->GetUnpackedSize($ArchFile));
  
?>