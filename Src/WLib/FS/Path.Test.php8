<?
  for($F=__FILE__; $F;) if(@include($F=DirName($F)).'/Using.php') break;
  
  $Loader->Load_Type('/FS/Path');

  $CP=T_FS_Path::Create('dissovet/');
  Echo $CP->ToString()."\n";
  $MP=T_FS_Path::Create('Zaschita');
  Echo $MP->ToString()."\n";
  $MP->PathFrom($CP);
  Echo $MP->ToString()."\n";
  $MP->Add('/index.html');
  Echo $MP->ToString()."\n";
?>